apiVersion: v1
kind: Service
metadata:
  name: mission-status-service
spec:
  selector:
    app: mission-status
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
