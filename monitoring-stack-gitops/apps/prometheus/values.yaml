prometheus:
  server:
    service:
      type: NodePort
      servicePort: 80
      nodePort: 30061
    persistentVolume:
      enabled: false

  alertmanager:
    enabled: false

  pushgateway:
    enabled: false
